services:
  - type: web
    name: football-scheduler-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 600 --bind 0.0.0.0:$PORT